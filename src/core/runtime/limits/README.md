# Limits

- **职责**：定义 RunLimits 并在副作用前检查预算。
- **非职责**：不实现超时调度或用户取消来源。
- **允许依赖**：State 与 Core 值类型。
- **禁止依赖**：具体 Adapter。
- **负责里程碑**：M2
- **当前状态**：M2 已实现步数、模型/工具调用、运行时长和 token 上限检查，并由 Runtime 在副作用边界执行。
